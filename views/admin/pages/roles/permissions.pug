extends ../../layouts/default.pug
block main
  if(role.permissions.includes("roles_permissions"))
    h1(class="mb-4") Phân quyền
    if(records)
      div(class="text-right")
        button(
          type="submit"
          class="btn btn-primary mb-3"
          button-submit=`/${prefixAdmin}/roles/permissions`
        ) Cập nhật
      table(
        class="table table-hover table-sm"
        table-permissions
      )
        thead
          tr
            th Tính năng
            each item in records
              th(
                class="text-center"
                role-id=item.id
              ) #{item.title}
        tbody
          //- Danh sách sản phẩm
          tr
            td(colspan="4")
              b Danh sách sản phẩm
          tr
            td Xem
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id 
                  data-name="products_view"
                  checked=(item.permissions.includes("products_view") ? true : false)
                )
          tr
            td Thêm mới
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="products_create"
                  checked=(item.permissions.includes("products_create") ? true : false)
                )
          tr
            td Chỉnh sửa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="products_edit"
                  checked=(item.permissions.includes("products_edit") ? true : false)
                )
          tr
            td Xóa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="products_delete"
                  checked=(item.permissions.includes("products_delete") ? true : false)
                )

          //- Danh mục sản phẩm
          tr
            td(colspan="4")
              b Danh mục sản phẩm
          tr
            td Xem
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id 
                  data-name="products-category_view"
                  checked=(item.permissions.includes("products-category_view") ? true : false)
                )
          tr
            td Thêm mới
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="products-category_create"
                  checked=(item.permissions.includes("products-category_create") ? true : false)
                )
          tr
            td Chỉnh sửa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="products-category_edit"
                  checked=(item.permissions.includes("products-category_edit") ? true : false)
                )
          tr
            td Xóa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="products-category_delete"
                  checked=(item.permissions.includes("products-category_delete") ? true : false)
                )

          //- Nhóm quyền
          tr
            td(colspan="4")
              b Nhóm quyền
          tr
            td Xem
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id 
                  data-name="roles_view"
                  checked=(item.permissions.includes("roles_view") ? true : false)
                )
          tr
            td Thêm mới
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="roles_create"
                  checked=(item.permissions.includes("roles_create") ? true : false)
                )
          tr
            td Chỉnh sửa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="roles_edit"
                  checked=(item.permissions.includes("roles_edit") ? true : false)
                )
          tr
            td Xóa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="roles_delete"
                  checked=(item.permissions.includes("roles_delete") ? true : false)
                )
          tr
            td Phân quyền
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="roles_permissions"
                  checked=(item.permissions.includes("roles_permissions") ? true : false)
                )

          //- Tài khoản admin
          tr
            td(colspan="4")
              b Tài khoản admin
          tr
            td Xem
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id 
                  data-name="accounts_view"
                  checked=(item.permissions.includes("accounts_view") ? true : false)
                )
          tr
            td Thêm mới
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="accounts_create"
                  checked=(item.permissions.includes("accounts_create") ? true : false)
                )
          tr
            td Chỉnh sửa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="accounts_edit"
                  checked=(item.permissions.includes("accounts_edit") ? true : false)
                )
          tr
            td Xóa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="accounts_delete"
                  checked=(item.permissions.includes("accounts_delete") ? true : false)
                )

          //- Bài viết 
          tr
            td(colspan="4")
              b Bài viết
          tr
            td Xem
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id 
                  data-name="blogs_view"
                  checked=(item.permissions.includes("blogs_view") ? true : false)
                )
          tr
            td Thêm mới
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="blogs_create"
                  checked=(item.permissions.includes("blogs_create") ? true : false)
                )
          tr
            td Chỉnh sửa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="blogs_edit"
                  checked=(item.permissions.includes("blogs_edit") ? true : false)
                )
          tr
            td Xóa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="blogs_delete"
                  checked=(item.permissions.includes("blogs_delete") ? true : false)
                )

          //- Danh mục bài viết
          tr
            td(colspan="4")
              b Danh mục bài viết
          tr
            td Xem
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id 
                  data-name="blogs-category_view"
                  checked=(item.permissions.includes("blogs-category_view") ? true : false)
                )
          tr
            td Thêm mới
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="blogs-category_create"
                  checked=(item.permissions.includes("blogs-category_create") ? true : false)
                )
          tr
            td Chỉnh sửa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="blogs-category_edit"
                  checked=(item.permissions.includes("blogs-category_edit") ? true : false)
                )
          tr
            td Xóa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="blogs-category_delete"
                  checked=(item.permissions.includes("blogs-category_delete") ? true : false)
                )

          //- Đơn hàng
          tr
            td(colspan="4")
              b Đơn hàng
          tr
            td Xem
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id 
                  data-name="orders_view"
                  checked=(item.permissions.includes("orders_view") ? true : false)
                )
          tr
            td Chỉnh sửa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="orders_edit"
                  checked=(item.permissions.includes("orders_edit") ? true : false)
                )
          tr
            td Xóa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="orders_delete"
                  checked=(item.permissions.includes("orders_delete") ? true : false)
                )

          //- Người dùng
          tr
            td(colspan="4")
              b Người dùng
          tr
            td Xem
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id 
                  data-name="users_view"
                  checked=(item.permissions.includes("users_view") ? true : false)
                )
          tr
            td Chỉnh sửa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="users_edit"
                  checked=(item.permissions.includes("users_edit") ? true : false)
                )
          tr
            td Xóa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="users_delete"
                  checked=(item.permissions.includes("users_delete") ? true : false)
                )

          //- Cài đặt chung
          tr
            td(colspan="4")
              b Cài đặt chung
          tr
            td Chỉnh sửa
            each item in records
              td(class="text-center")
                input(
                  type="checkbox" 
                  data-id=item.id
                  data-name="setting_edit"
                  checked=(item.permissions.includes("setting_edit") ? true : false)
                )